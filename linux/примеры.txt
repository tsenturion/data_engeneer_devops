helm completion bash | sudo tee /etc/bash_completion.d/helm > /dev/null
exec bash
helm ins tab

helm repo add bitnami https://charts.bitnami.com/bitnami
helm repo update
helm search repo nginx

bitnami/nginx
bitnami/nginx-ingress-controller
bitnami/nginx-intel

CHART VERSION: 18.1.3
APP VERSION: 1.27.0

helm install my-realease bitnami/nginx -n web --create-namespace

STATUS: deployed
REVISION: 1

kubectl get all -n web

pod/
service/
deployment/
replicaset/

TYPE: LoadBalancer
EXTERNAL-IP: 158.160.x.x

custom-value.yaml
replicaCount: 2
staticSiteConfigmap: "index"

{{ .Values.replicaCount }}


kubectl apply -f configmap-index.yaml -n web
helm upgrade my-release bitnami/nginx -n web -f custom-value.yaml

REVISION: 2

kubectl get all -n web


helm history my-release -n web

REVISION: 1 Install complete
REVISION: 2 Upgrade complete

helm rollback my-release 1 -n web

REVISION: 3 Rollback to 1

helm upgrade my-release bitnami/nginx --atomic

helm uninstall my-release -n web
kubectl delete configmap index -n web
--create-namespace